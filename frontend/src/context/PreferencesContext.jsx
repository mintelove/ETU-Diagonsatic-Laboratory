import { createContext, useCallback, useContext, useEffect, useMemo, useState } from 'react';
import { api } from '../api/client.js';
import { useAuth } from './AuthContext.jsx';
const PreferencesContext=createContext(null),defaults={theme:'light',language:'en',timeFormat:'24',dateFormat:'locale',notifications:true,sidebarCollapsed:false},words={en:{dashboard:'Dashboard',settings:'Settings',signOut:'Sign out',reception:'Reception desk',collection:'Sample collection',approvals:'Extra approvals',stock:'Stock management',patients:'Patient management',samples:'Sample types',categories:'Stock categories',users:'User management',light:'Light mode',dark:'Dark mode',language:'Language',theme:'Theme',preferences:'Preferences'}};
export function PreferencesProvider({children}){const {user,token}=useAuth(),[preferences,setPreferences]=useState(()=>({...defaults,...JSON.parse(localStorage.getItem('etu_preferences')||'{}')}));useEffect(()=>{if(user?.preferences)setPreferences(current=>({...current,...user.preferences}))},[user]);useEffect(()=>{document.documentElement.dataset.theme=preferences.theme;document.documentElement.lang=preferences.language==='am'?'am':'en';localStorage.setItem('etu_preferences',JSON.stringify(preferences))},[preferences]);const updatePreferences=useCallback(async updates=>{const previous=preferences,next={...preferences,...updates};setPreferences(next);if(token)try{const result=await api('/preferences',{token,method:'PATCH',body:JSON.stringify(updates)});setPreferences(current=>({...current,...result.preferences}))}catch(error){setPreferences(previous);throw error}},[preferences,token]);const value=useMemo(()=>({preferences,updatePreferences,t:key=>words.en[key]||key}),[preferences,updatePreferences]);return <PreferencesContext.Provider value={value}>{children}</PreferencesContext.Provider>};export const usePreferences=()=>{const context=useContext(PreferencesContext);if(!context)throw new Error('PreferencesProvider is missing.');return context};
