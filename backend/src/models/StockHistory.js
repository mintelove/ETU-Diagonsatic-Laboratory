import mongoose from 'mongoose';
const historySchema = new mongoose.Schema({ item: { type: mongoose.Schema.Types.ObjectId, ref: 'StockItem', required: true, index: true }, action: { type: String, enum: ['Created', 'Updated', 'Deleted', 'Quantity Changed', 'Automatic Deduction', 'Manual Deduction'], required: true }, field: String, previousQuantity: Number, quantityDeducted: Number, newQuantity: Number, reason: { type: String, trim: true, maxlength: 300 }, user: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true }, patient: { type: mongoose.Schema.Types.ObjectId, ref: 'Patient' }, patientId: String, testName: String, orderedTests: [String], snapshot: mongoose.Schema.Types.Mixed }, { timestamps: { createdAt: 'createdDate', updatedAt: false }, versionKey: false });
historySchema.index({ item: 1, createdDate: -1 });
export default mongoose.model('StockHistory', historySchema);
