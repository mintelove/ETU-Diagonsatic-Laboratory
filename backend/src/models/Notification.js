import mongoose from 'mongoose';
const notificationSchema = new mongoose.Schema({ recipient: { type: mongoose.Schema.Types.ObjectId, ref: 'User', required: true, index: true }, item: { type: mongoose.Schema.Types.ObjectId, ref: 'StockItem' }, entity:{type:mongoose.Schema.Types.ObjectId},entityType:{type:String,default:'StockItem'}, type: { type: String, enum: ['Low Stock', 'Critical Stock', 'Critical Emergency','New Approved Report','Patient Returned','Counselling Reminder','System Announcement','Critical Laboratory Message','Reception Stock Edit Request','Critical Stock Deficit'], required: true }, message: { type: String, required: true, maxlength:500 }, read: { type: Boolean, default: false } }, { timestamps: { createdAt: 'createdDate', updatedAt: false }, versionKey: false });
notificationSchema.index({ recipient: 1, read: 1, createdDate: -1 });export default mongoose.model('Notification', notificationSchema);
