import {Router}from'express';import {requireAuth,allowRoles}from'../middleware/auth.js';import {ROLES}from'../constants/roles.js';import {pdf,csv,document}from'../controllers/finalReportController.js';const router=Router();router.use(requireAuth,allowRoles(ROLES.RECEPTION,ROLES.ADMIN,ROLES.SAMPLE_COLLECTOR,ROLES.APPROVER));router.get('/:id',document);router.get('/:id.pdf',pdf);router.get('/:id.csv',csv);export default router;
