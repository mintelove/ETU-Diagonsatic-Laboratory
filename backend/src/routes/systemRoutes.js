import {Router}from'express';import {z}from'zod';import {requireAuth,allowRoles}from'../middleware/auth.js';import {validate}from'../middleware/validate.js';import * as c from'../controllers/systemController.js';
const router=Router(),color=z.string().regex(/^#[0-9a-fA-F]{6}$/),theme=z.object({primary:color,secondary:color,sidebar:color,card:color,header:color,button:color,background:color,textAccent:color,scope:z.enum(['me','all']).default('all')});
router.use(requireAuth);router.get('/theme',c.getTheme);router.use(allowRoles('Admin'));router.put('/theme',validate(theme),c.setTheme);router.post('/reset/preview',validate(z.object({password:z.string().min(1),selected:z.array(z.string()).min(1)})),c.previewReset);router.post('/reset/execute',validate(z.object({password:z.string().min(1),secondPassword:z.string().min(1),phrase:z.string(),selected:z.array(z.string()).min(1)})),c.executeReset);export default router;
