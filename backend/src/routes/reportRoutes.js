import { Router } from 'express'; import { requireAuth, allowRoles } from '../middleware/auth.js'; import { ROLES } from '../constants/roles.js'; import { exportCsv, exportExcel, exportPdf } from '../controllers/reportController.js'; const router=Router();router.use(requireAuth,allowRoles(ROLES.ADMIN));router.get('/stock.csv',exportCsv);router.get('/stock.xlsx',exportExcel);router.get('/stock.pdf',exportPdf);export default router;
