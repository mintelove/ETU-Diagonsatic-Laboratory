import {Router}from'express';import {requireAuth,allowRoles}from'../middleware/auth.js';import {ROLES}from'../constants/roles.js';import {validate}from'../middleware/validate.js';import {requestSchema,reportSchema}from'../validators/collectionValidators.js';import * as c from'../controllers/collectionController.js';
const router=Router();router.use(requireAuth,allowRoles(ROLES.SAMPLE_COLLECTOR));router.get('/dashboard',c.dashboard);router.get('/queue',c.queue);router.get('/reports',c.reports);router.delete('/reports/:id',c.deleteDraft);router.get('/patients/:patientId/history',c.history);router.post('/patients/:patientId/start',c.beginCollection);router.get('/stock',c.stock);router.post('/extra-requests',validate(requestSchema),c.extraRequest);router.get('/equipment',c.equipmentParameters);router.put('/patients/:patientId/report',validate(reportSchema),c.saveReport);router.post('/patients/:patientId/report/submit',c.submitReport);router.get('/counselling/queue',c.counsellingQueue);router.get('/counselling/history',c.counsellingHistory);router.put('/counselling/:id',c.saveCounselling);router.post('/counselling/:id/complete',c.completeCounselling);export default router;
