import {Router}from'express';import {z}from'zod';import {requireAuth,allowRoles}from'../middleware/auth.js';import {ROLES}from'../constants/roles.js';import {validate}from'../middleware/validate.js';import * as c from'../controllers/reportApprovalController.js';
const router=Router(),schema=z.object({status:z.enum(['Approved','Rejected']),comments:z.string().trim().max(2000).optional().default('')}).superRefine((value,ctx)=>{if(value.status==='Rejected'&&!value.comments)ctx.addIssue({code:z.ZodIssueCode.custom,path:['comments'],message:'A reason for rejection is required.'})});router.use(requireAuth,allowRoles(ROLES.ADMIN,ROLES.APPROVER));router.get('/pending',c.pending);router.get('/history',c.history);router.patch('/:id',validate(schema),c.decide);export default router;
