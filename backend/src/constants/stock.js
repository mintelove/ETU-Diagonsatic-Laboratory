export const DEFAULT_CATEGORIES = ['Test Tubes','Syringes','Gloves','Urine Cups','Stool Cups','Slides','Cover Slips','Neck Tubes','Blood Collection Tubes','Vacutainers','Cotton','Alcohol Swabs','Gauze','Disposable Masks','Disposable Aprons','Biohazard Bags','Other Laboratory Consumables'];
export const UNITS = ['Piece','Box','Pack','Bottle','Roll','Pair','Carton','Bag','Set'];
export function stockLevel(item) { const remaining = item.currentQuantity - item.usedQuantity; const pct = item.currentQuantity ? Math.max(0, Math.round((remaining / item.currentQuantity) * 100)) : 0; if (remaining <= 0) return { key: 'Out of Stock', label: 'Out of Stock', percentage: 0 }; if (pct < 5) return { key: 'Critical Emergency', label: 'Critical Emergency', percentage: pct }; if (pct <= 5) return { key: 'Critical', label: 'Critical', percentage: pct }; if (pct <= 20) return { key: 'Low', label: 'Low Stock', percentage: pct }; if (pct <= 74) return { key: 'Moderate', label: 'Moderate', percentage: pct }; return { key: 'Healthy', label: 'Healthy Stock', percentage: pct }; }
